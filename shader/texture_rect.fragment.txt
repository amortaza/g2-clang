#version 330

in vec2 vsTexCoord;

out vec4 outFragColor;

uniform sampler2D uSampler;
uniform float uAlpha;

void main()
{
    outFragColor = texture2D(uSampler, vsTexCoord.st) * vec4(1,1,1,uAlpha);
}
